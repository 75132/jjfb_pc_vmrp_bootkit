# 给 Cursor 的最短入口

1. 在新 clean 仓库中复制本包 `cursor/.cursor/`。
2. 把 `CURSOR_MASTER_PROMPT.md` 全文发给 Cursor。
3. 让 Cursor 只执行 `Phase 0 / Task 0.1`。
4. 不把旧修改版 `bridge.c` 复制进新仓库。
5. 每轮跑 `tools/audit_launcher_core.py`。

路线口令：

> 解析 shell，重建 launch contract；模拟平台，不扶游戏状态。
