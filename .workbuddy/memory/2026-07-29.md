# 2026-07-29

## P9: 0x304BF0 真实 epilogue 定位 + resume-to-epilogue A/B 方案
- 前沿：P8 A/B 已落地，`direct_lr` 为 product-safe 基线（5 BMP 完成/首帧稳定/SP delta=0）；`callsite` 模式不安全(Situation C)。
- 交付：`reports/p9_304bf0_epilogue.md` + `research/runners/p9_disasm_304bf0_epilogue.py`（capstone，解码头在 `out/research_p9_extract/robotol.ext`）。
- 结论：0x304BF0 是带 PUSH{r0-r7,lr}+SUB SP,#0xBC 的函数入口；唯一返回点 = 0x304C4A `add sp,#0xcc` + 0x304C4C `pop{r4,r5,r6,r7,pc}`；栈净 224↔224 平衡。
- resume-to-epilogue 安全：SP=g_entry_sp-224，PC=0x304C4B(Thumb)，R0=status；与 direct_lr 栈等价，可替代不安全的 callsite。
- 下一步（需 i686-mingw+unicorn 工具链，本环境缺）：在 `restore_304bf0_ok()` 加 `epilogue` 分支 → 构建+单测+反跑偏审计 → 黄金链验收(SP delta=0/5资源/首帧稳定) → 通过则淘汰 callsite。
- 注意：本环境无 32 位 MinGW 与 unicorn 开发库，未改 clean core（避免破坏已通过基线且无法审计）。

## 真正启动 jjfb 程序与资源包（落地验证）

- 目标从"研究推进"转为"真正启动 jjfb + 资源包"。工具链本环境可用（i686 MinGW + Unicorn 1.0.2）。
- 验证一（产品直驱强成功）：`RUN_PRODUCT_DIRECT_JJFB.ps1 -SkipBuild -SkipVmrpBuild -Seconds 150` → 11 门禁全 yes、禁用项全 clean、SCHEDULER_NATURAL_CALLBACK=yes(forced=no)。main.exe sha256 b784ab50...。guest 推进到 BOOT_FAMILY_HANDLER_ENTER a=0x1E209 c=0x30D301，`direct_lr` 安全模式生效。
- 验证二（Layer-1 首帧门禁 PASS）：`RUN_JJFB_LAUNCHER.ps1 -SkipBuild -SkipVmrpBuild -HoldSeconds 150` → 首帧 BMP 写出（240x320, nonBlackPct=7.08%, 16 色），已渲染资源成员 loadingbar/bar/textbar/topleft/topright。runtime_progress 序列：waiting_for_first_frame → pending_bitmap_commit ok → drawfp_first_drawn → UI_BUILDER_LEAVE_CANDIDATE。
- 关键产物：`out/visual_baseline/gate_20260729_173820/launcher_first_frame.bmp` + `layer1_gate.json` + `runtime_progress.jsonl`；`reports/product_direct_jjfb_verdict.md`；`reports/launch_jjfb_20260729.md`；`logs/product_direct_jjfb_vmrp.txt`。
- 注意：30s 挂起会卡在 bootstrap(PLAT_10140)，guest 约需 50-60s 才进 family handler 并绘首帧；150s 稳定通过。产物均为 7/28 23:38 构建，未改 clean core。
- P9 epilogue 模式的实现/运行仍待带工具链处落地；当前 direct_lr 基线已 product-safe 通过验收。
