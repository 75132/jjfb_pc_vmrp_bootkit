# cfg.bin index 36 原始记录审计

- cfg SHA-256：`0d5837d251e9e0bbfb82495c0b73fd32ea4bcb5d817ca55495c35a23e8b7a806`
- 经验记录位置：`1024 + 36 × 272 = 10816`
- 经验记录大小：272 字节
- 布局置信度：`empirical_record36`，尚不能当成所有 GWY cfg 的通用规范
- 图标：`ng_jjfb.gif`（偏移 `0x40`）
- `napptype`：`12`（U8，偏移 `0x57`）
- 旧四字节字段：`g:u2`（偏移 `0x58`，**不是**有效 48 字节 `sdk_key.dat`）
- 固定标题切片：`风暴(火爆公测)`（UTF-16BE，偏移 `0x5C`）；“机甲”前缀不在该切片中，解析器不会擅自补字
- `nextid`：`482`（观察到的 24-bit BE，偏移 `0x72`）
- `ncode`：`512`（观察到的 24-bit BE，偏移 `0x78`）
- `narg`：`0`（观察到的 24-bit BE，偏移 `0x7B`）
- `narg1`：`1`（U8，偏移 `0x7E`）
- 目标：`gwy/jjfb.mrp`（从 `0x7F` 起）

> 这次复核纠正了早期工具把 `0x74/0x78` 当 LE32 导致读成 `226/512` 的错误。新开发必须把布局标记为“经验版本”，通过更多记录、更多 cfg 和 shell 读取代码继续验证，不能把 index 36 的偏移直接当全局格式标准。
