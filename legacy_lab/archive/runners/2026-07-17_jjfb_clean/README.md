# Archived root runners (2026-07-17)

Moved out of repo root during JJFB clean baseline tidy.

Kept at root instead:

- `RUN_JJFB.ps1` — primary entry
- `RUN_V71_PRESENT_COALESCE.ps1` — implementation

These scripts are historical probes (Path A / enqueue / chrome / other games).
Do not treat as the main GWY launcher route.
